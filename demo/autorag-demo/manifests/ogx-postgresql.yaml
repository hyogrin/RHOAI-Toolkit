---
apiVersion: v1
kind: Secret
metadata:
  name: ogx-postgres-secret
  namespace: ${NAMESPACE}
type: Opaque
stringData:
  password: ogxserver123
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ogx-postgres-pvc
  namespace: ${NAMESPACE}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ogx-postgres
  namespace: ${NAMESPACE}
  labels:
    app: ogx-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ogx-postgres
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: ogx-postgres
    spec:
      containers:
        - name: postgres
          image: registry.redhat.io/rhel9/postgresql-16:latest
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRESQL_USER
              value: ogx
            - name: POSTGRESQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ogx-postgres-secret
                  key: password
            - name: POSTGRESQL_DATABASE
              value: ogx
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/pgsql/data
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: ogx-postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: ogx-postgres
  namespace: ${NAMESPACE}
spec:
  selector:
    app: ogx-postgres
  ports:
    - port: 5432
      targetPort: 5432
